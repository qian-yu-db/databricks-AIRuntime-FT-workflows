[pytest]
# lib/ and scripts/ on the path so tests import the real modules directly
# (extract_eval, run_sweep, eval_cli) — no packaging, no sys.path hacks.
testpaths = tests
pythonpath = lib scripts
addopts = -q
